---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title?: string;
  lang?: string;
}

const { title, lang } = Astro.props;
---

<BaseLayout title={title} lang={lang}>
  <slot />
</BaseLayout>
